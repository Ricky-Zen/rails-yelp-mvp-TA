
        <div class="col review w-50 mb-10">
          <h5 style="text-align:center" class="mb-4">Add a review</h5>
          <%= simple_form_for [@restaurant, @review] do |f| %>
            <%= f.input :content %>
            <%= f.input :rating %>
            <%= f.submit 'Create it!', class: 'btn btn-primary' %>
          <% end %>
        </div>

